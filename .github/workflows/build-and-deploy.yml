name: Build, Sign and Push Robot App

on:
  workflow_dispatch:
    inputs:
      app_version:
        description: 'Application version (e.g., 1.0.0)'
        required: true
        type: string
        default: '1.0.0'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/robot-app

jobs:
  build-sign-deploy:
    permissions:
      contents: read
      id-token: write

    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set APP_VERSION
        id: version
        run: |
          echo "APP_VERSION=${{ github.event.inputs.app_version }}" >> $GITHUB_ENV
          echo "APP_VERSION=${{ github.event.inputs.app_version }}" >> $GITHUB_OUTPUT
          echo "Using APP_VERSION: ${{ github.event.inputs.app_version }}"

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install cosign
        uses: sigstore/cosign-installer@v3.1.1
        with:
          cosign-release: 'v2.2.0'

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: ./robot
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.APP_VERSION }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          build-args: |
            APP_VERSION=${{ env.APP_VERSION }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Sign the published Docker image
        env:
          TAGS: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.APP_VERSION }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          echo "Signing image: ${TAGS}@${DIGEST}"
          cosign sign --yes "${TAGS}@${DIGEST}"

      - name: Verify the signed Docker image
        env:
          TAGS: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.APP_VERSION }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          echo "Verifying signature for: ${TAGS}@${DIGEST}"
          cosign verify "${TAGS}@${DIGEST}" --certificate-identity-regexp=".*" --certificate-oidc-issuer-regexp=".*"

      - name: Output image details
        run: |
          echo "🚀 Docker image built and pushed successfully!"
          echo "📦 Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.APP_VERSION }}"
          echo "🔐 Image digest: ${{ steps.build.outputs.digest }}"
          echo "✅ Image signed with cosign"
          echo "📝 Version: ${{ env.APP_VERSION }}"